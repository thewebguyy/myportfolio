'use client'

// Testimonials section — intentionally dormant until real testimonials are available.
// Add real entries (verifiable LinkedIn URLs, real people) to lib/testimonials.ts,
// then restore the JSX in this file and re-add <Testimonials /> to app/page.tsx.
export function Testimonials() {
  return null
}
